window.addEventListener('load', ()=>{
    let button = document.querySelector('.chat-button')

    button.addEventListener('click', (e)=>{
        e.preventDefault()

        //Programa aquí lo que necesites
        alert('Mostrando botón')
    })
})